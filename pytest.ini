[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = tests
addopts = -q --strict-markers -m "not live"
markers =
    live: hits the real external routing API; run with -m live
